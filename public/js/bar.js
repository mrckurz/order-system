import { startStation } from './station.js';

// The Bar/Schank display shows the "drinks" station (no printing).
startStation({ station: 'drinks', titleKey: 'bar', accentClass: '', showReprint: false });
